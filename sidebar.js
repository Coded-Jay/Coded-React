import { useState } from "react";
import "./sidebar.css";

const navItems = ["home", "Services", "Customer Care", "Sign Up"];
export const Sidebar =()=> {
    const [isOpen, setIsOpen] = useState(false);
    return(
    );
}